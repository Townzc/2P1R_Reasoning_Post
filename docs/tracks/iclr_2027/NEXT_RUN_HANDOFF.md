# Next run: the existing E017 only

September 11, 2026. **Prepared, not executed.** The existing authorization is
unchanged and does not need to be requested again. This milestone performed
document/source inspection only, with no compute-server contact or new model
call. The last verified provider state remains stopped; no fresh provider check
is implied. Local connection and transfer locations belong in private project
coordination, not this public handoff.

## Offline handoff assessment

The [existing readiness report](../../../reports/REAL_MATH_E017_READY.md) and
[registration](../../experiments/E017_task_completion_calibration.md) already
cover input identity, stop-event semantics, independent raw-record auditing,
process limits, compact export and confirmed shutdown. No additional missing
experimental check was identified. Their 62 CPU passes, 160-stream replay and
clean-checkout transfer were not rerun just to create this research track.
The two GNU-timeout integrations still need the actual Linux environment.

There is one **source-routing issue introduced by separate research branches**:
the frozen [provenance function](../../../analyses/e017.py) requires a clean
tracked tree and `HEAD == origin/main`. A newly published documentation branch
does not satisfy that guard. Do not weaken it, regenerate inputs, change the
historical dependency freeze, or relabel a branch tip as `origin/main`.

The compatible route is to execute the unchanged E017 release from its existing
published main snapshot, separately from research-document development. The
verified main tip at this milestone is
`73170a459e9fed2c5fdea6a9afb40b4b80837bb2`. It contains implementation
`b365bd8bc9fca6521946a83c5ed6565fce6a4e45` and input release
`410ee3e99979402803286901aa9bab6bf00b2247`. Stage that exact published history in
the existing server execution checkout after owner startup, preserving unique
outputs. Research results can be imported and committed on the assigned research
branch afterward; no merge or push to main is needed.

Resolve publication/routing **offline before startup**. If true remote main has
advanced, reconcile the exact published execution commit and all frozen runtime
dependencies before opening a paid window. A blocked guard is a reason to stop,
not to spoof a ref or debug indefinitely on the rental. Do not change the original
local repository or another research worktree for this route. The already
verified private final bundle is reusable; a new branch is not a reason to
recreate it or repeat passed release tests.

The new worktree does not inherit ignored private assets. The existing final
bundle and current ledger recovery snapshots have been located in the retained
local archive; both ledger copies match the recorded current hash. Their exact
locations are recorded only in local coordination. Restore from that verified
snapshot when needed, never from a guessed default path or a newly initialized
allowance. This resolves a local-path handoff detail, not a new experiment check.

## Exact admitted work

| Item | Frozen value |
| --- | --- |
| Run ID | `gsm8k_stop_e017_r1`; reject an existing completed or incomplete attempt at that ID |
| Model | Original Qwen/Qwen2.5-1.5B base, revision `8faed761d45a263340a0528343f099c05c9a4323` |
| Inputs | The 64 observed E016 parents, development ranks 17–80, original order and prompts |
| Decoder | Greedy, seed17, batch8, context1024, cap768; fixed batch membership; FP32/BF16 autocast/SDPA, TF32 off |
| Change | Per-row stop at true native EOS or the earliest complete recognized new-question boundary; retain trigger and record later batch padding separately |
| Excluded | Training, E015 weights load, teacher generation, new checkpoint, new prompt, reserved dev, final test, automatic retry or follow-up |
| Release SHA256 | `daeb946c7e3d01afe54552224770f3429129ab05022adeb76138222b87ce56af` |
| Process ceiling | 240 seconds plus 15-second termination guard = 255 seconds |
| Rental ceiling | 15 minutes/CNY2 at CNY8/hour; target12 minutes/CNY1.60 |

The canonical historical state is **20 complete receipts, 6,921 used of 7,200
seconds, 279 remaining, zero reservations**, with ledger SHA256
`c33623087b752f0bbc82a32bb90ac8f11a489fc910b2ebdaacb57000c0893106`.
An old clone or fresh worktree must not initialize a new ledger. E017's maximum
charge leaves at least24 seconds; actual charge is determined by its receipt.
Only one coordinator may mutate the canonical ledger. Publish a sanitized
reconciliation and retain its independent private backup after this run.

## Finite execution and exit

1. After owner startup, confirm the exact existing instance, recorded original
   model/software, idle A80080GB, and at least256MiB scratch space. Preserve the
   E015 volume. Record provider power-on time or explicitly label owner
   notification as a proxy. Install the already specified provider shutdown
   backstop before generation.
2. Use the published execution checkout described above. Verify its source,
   input release and current ledger. Run the registered focused Linux suite,
   including both pending watchdog checks; this is the existing startup
   requirement, not a new local regression campaign.
3. At admission require555 seconds until the rental deadline:255 guarded
   process +120 export +180 shutdown/slack. Do not launch after345 elapsed
   seconds. If preflight fails or runs late, retain the failure evidence and
   end the window promptly.
4. Run the fixed commands from [the original handoff](../../NEXT_SESSION.md)
   in the verified server execution directory. Paths and timestamps must be
   verified session values. Default inspection is not a model run; only the
   accepted `launch --execute` performs E017. Do not call `prepare` again.
5. Preserve and independently export raw full batch/prefix IDs, stop receipts,
   predictions, metrics, effective generation configuration, profiles, phase
   timings, run/source manifest, stdout and process receipt, plus the updated
   canonical ledger. The registered audit checks completed receipt consistency.
   Export failures must remain visible.
6. Confirm provider shutdown; Python exit or SSH loss is insufficient. Cancel
   the temporary backstop after confirmation. Complete local output auditing,
   analysis, scientific result writing and branch publication with the server
   off. Do not run E018 or another prompt in this rental.

Required result table: all64 records; parsed and correct-completed counts;
native EOS, boundary stops, true length caps, invalid stops and false-EOS counts;
retained tokens, batch padding, measured generation/runtime/memory; historical
E016 metrics alongside the new contract without overwriting either. Prefix
differences from old GPU outputs must be retained, not treated as a failed
promise of exact numerical replay.

## Result-dependent next action

| Outcome | Next step |
| --- | --- |
| Complete and usable: at least48 parsed, at least8 task-correct, at most8 length caps, no invalid/false-EOS receipts | Publish actual E017 measurements and review P006 B/C using [the concrete packet](NEXT_PHASE_REVIEW.md). It does not authorize training automatically |
| Complete but usability fails | Preserve the failure; review one diagnostic or output-contract alternative. Do not widen caps, change prompts, or rescore the old run in place |
| Incomplete, timeout, source/audit mismatch, or missing export | Record execution failure and actual charge, reconcile the ledger, confirm shutdown, then diagnose offline. Do not reuse the run ID or retry automatically |

E017 does not recover E015 weights. The latter's complete12-file/6.19GB
checkpoint still lacks an independent full copy. Preserve the stopped instance
and follow the separate [recovery plan](../../E015_RECOVERY_PLAN.md), with a fresh
retention-deadline check before recovery. The old approximate September25 date
is not a verified current retention deadline. No recovery start, deletion,
release, or full-weight copy is part of this handoff milestone.
