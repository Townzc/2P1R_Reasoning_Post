# E010 — Frozen identity-absent allocation pair, seed31

**Execution entry date:** 2026-09-09 UTC. **Entry status:** owner explicitly
authorized execution on the supplied, restarted A800; server recovery and Linux
preflight in progress; no new model outcome available at registration.

## Question, prior failures and design timing

Does four-path allocation improve matched-development greedy expression
correctness over one-path repetition within C012's selected identity-absent
population, at the same questions, complete dose and per-update structure/token
histograms? C012 selected this boundary before its support counts or any new
model outputs. This execution record implements that frozen plan without a new
scientific factor or endpoint. See [C012](C012_identity_absent_boundary_preparation.md)
and [the exact training protocol](../ABSENT_BOUNDARY_TRAINING.md).

The previous same-pool seed17/23 primary gaps were both five of64 questions,
but broader transfer remained unsupported. C009's capped search did not measure
capacity; C010 timed out with incomplete support; C011/C013 show that exact
two-family matching itself excludes large parts of the original population.
C012 removes requirements for the unused identity-present family while keeping
within-question length and shared four-structure blocks. It does not eliminate
all selection or numerical-difficulty differences.

## Immutable recipe, endpoints and controls

- Data: `runs/absent_boundary_seed31_20260909_r1`; manifest SHA256
  `cd9a72d187dbb18f2b75b743ccc0cf85c94951ee574c07e55ed8ddf1f4a1ab97`.
- Queue: `configs/absent_boundary_seed31/queue.json`; run IDs
  `absent_boundary_paths_seed31_r1` and `absent_boundary_gcm_seed31_r1`.
-128 questions,32 blocks,8 cycles,1024 updates,4096 presentations per arm;
  batch4/microbatch2. Paths uses four trajectories eight times each per question;
  GCM uses one trajectory32 times. Selection/assignment/order/training seed31;
  evaluation seed17. Both start from the pinned Qwen2.5-1.5B base.
-277760 supervised EOS-inclusive response tokens,482848 nonpadding processed
  tokens and3734 padding tokens per arm. Same ordered questions and token,
  structure and operator histograms at every update. FP32 AdamW parameters,
  BF16 autocast,LR5e-5,WD0.01,clip1; sequence/generation limit384.
- Primary: signed Paths-minus-GCM greedy final-expression correctness on the
  unchanged64 matched-dev questions, including every paired cell. Always report
  the unchanged64 broader-dev questions. Complete displayed-trace validity,
  fixed descriptive strata and sampled pass@1/2/4 are secondary. The2048 raw
  holdout groups remain unsolved and unevaluated.

## Authorization, restoration and stopping rule

The owner's latest message authorizes this finite pair now; the historical
morning handoff's wait-for-launch condition is satisfied. SSH key authentication
works. The original ledger is4716/7200 charged,2484 remaining,13 receipts and
zero reservations, SHA256
`995d1ec3d484671bb391f3997640712201d6341b97a00e1feafed5378b22633e`.
The pair reserves2130 seconds:1050 process seconds plus15 guard per arm.
No calibration replay, third model, automatic retry or dose shortening is added.
A timeout, nonfinite training, mismatched invariant or incomplete dose stops the
queue and is retained. Neither favorable nor adverse validation changes the dose.

Recover current published Git history, run the real-tokenizer Linux suite,
verify pinned model/data bytes, reuse the completed calibration with the same
recipe, confirm an idle GPU and at least18GiB free, then launch through the
existing persistent bounded queue. SciPy1.18.0 is added only for CPU diagnostics;
PyTorch2.8.0+cu128, NumPy2.3.2 and NetworkX3.5 retain their measured GPU pins.
The execution commit must be published and clean on the server before either
arm starts; both arms use that same commit.

## Interpretation and preservation

A favorable result is one exploratory allocation contrast in a selected
absence-only population. An adverse/null result narrows the old observation.
Neither sign identifies the effect of removing identity operations, because
the old and new populations/exposure distributions differ. Identity absence
still permits cancellation/computed constants, and equal syntax is not equal
semantic difficulty. This result alone cannot establish an ICLR-level claim.

After execution, independently audit all800 predictions and full-dose receipts,
publish raw compact artifacts and analysis for both development sets, verify
separate checkpoint and cumulative-ledger backups, then perform the previously
authorized normal shutdown. Never delete/release the instance as shutdown.

## Preflight appended before launch

All294 Linux tests passed in22.975 seconds, including both GNU-timeout budget
integration checks and the real pinned-tokenizer tests. All nine pinned model
files and the complete frozen data passed independent verification. The queue
inspection requires2130 of2484 remaining seconds; the completed calibration is
reused. The GPU/process scan is empty, all new run IDs are absent and
31,613,050,880 data bytes are free. See the [immutable preflight receipt](../../reports/a800_absent_preflight_20260909_r1/startup.json),
[tests](../../reports/a800_absent_preflight_20260909_r1/tests.log) and
[data audit](../../reports/a800_absent_preflight_20260909_r1/integrity.json).
Only registration/status text and these receipts change after that tested source.

## Outcomes — pending

Append measured results, source commit, exact paired counts, failures, dose,
resource charges, interpretation and recovery/publication receipts here after
completion. Preserve the entry above as the pre-execution record.

## Launch appended —2026-09-09T18:24:48 UTC

The finite queue launched independently of SSH from the clean, prepublished
commit `9217685f6bdb4d0c67a0d76513c12f89898593cb`. The first arm is training;
no final outcome or success is implied by launch. See the [launch receipt](../../reports/absent_boundary_seed31_launch_20260909.json).
A prior launcher-shell attempt found no bare `python3` on the noninteractive
remote PATH and exited before creating any job or reservation; using the verified
absolute interpreter resolved it. No model attempt or GPU charge arose from
that shell error. The existing shutdown follow-up now covers E010 only, with
publication, independent backups and an idle/unreserved server required first.

## Completion appended —2026-09-09T18:41:53 UTC

Both runs completed from `9217685f6bdb4d0c67a0d76513c12f89898593cb` at the full
registered dose. Matched greedy final-expression correctness is **7/64 versus5/64**:
3 both correct,4 Paths only,2 GCM only,55 neither. Matched complete traces are
**4/64 each**; broader expressions and complete traces are **0/64 versus2/64**.
Matched sampled pass@1/2/4 are.09375/.13020833/.171875 versus
.0703125/.08854167/.109375. Complete sampled traces are13/256 versus15/256.
The [full analysis](../../reports/ABSENT_BOUNDARY_SEED31_RESULTS.md) preserves
every primary/secondary endpoint, fixed strata, error counts and limitations.

Server and independent local audits of all800 saved predictions are byte-identical.
Every planned update and all277760 supervised tokens per arm match; no GPU
failure, retry, nonfinite history, output truncation or dose selection occurred.
Peak allocated memory is26857.17MiB for each arm. Both charge512 process-seconds:
1024 this phase,5740/7200 cumulative,1460 remaining,15 reconciled receipts and
zero reservations. The prior13 ledger jobs are unchanged.

**Analysis/decision:** the primary net two-question advantage has no complete-trace
or broader-development support. Retain this weak exploratory boundary result;
do not expand a positive claim, rerun to seek a preferred sign or infer a null
effect. Differences from seed17/23 cannot identify an identity-operation mechanism
because training questions, path population, exposures, tokens and seed changed.
The next proposed work is CPU task/estimand design and support/shortcut audits,
followed by owner review before any new scientific GPU phase.1460 remaining
seconds cannot reserve another2130-second pair under the present limits.

At this results milestone all12 Paths checkpoint files are independently
SHA256-verified; GCM backup and final normal shutdown remain pending.

## Independent preservation completed

Both final checkpoints are independently backed up:24 files totaling
12,381,607,162 bytes, all SHA256-verified. The raw result/audit milestone
`3585ad2d2f6424180b4b3ec345904dc0fc21fea6` is synchronized cleanly to the server.
The latest ledger is independently reconciled and a fresh server check shows
no GPU/training process or reservation. See the [backup summary](../../reports/absent_boundary_seed31_checkpoint_backup_summary.json).
The Mac is locked, so final provider stopped-state confirmation is pending.
The authorized vendor shutdown request is distinct from that confirmation.

## Vendor shutdown requested; console verification pending

After preservation was published at `b27a85c` and synchronized by a verified
incremental Git bundle, fresh instance/source/GPU/ledger checks passed. The
vendor shutdown helper returned0 at19:00:27 UTC; SSH was unavailable at19:01:07.
See the [request receipt](../../reports/absent_boundary_seed31_shutdown_request.json).
An earlier GitHub pull timeout triggered a stale-HEAD precondition rejection,
so no premature shutdown occurred; that operational recovery is retained.
The Mac remains locked. Final provider stopped state is unverified, and the
existing heartbeat only follows up on console confirmation after unlock.
