# C012 — Identity-absent Paths/GCM boundary candidate, CPU preparation

**Registered 2026-09-09 UTC before this candidate's support or model outcomes.**
The user requested complete CPU search, addressing matching selection, and a
training plan ready for the next morning. The server remains shut down. This
entry freezes the candidate and preparation decisions; it does not execute a
model or authorize a larger grid.

## Estimand and reason for removing irrelevant constraints

The next candidate is **one identity-absent Paths/GCM pair**, chosen before new
support counts. Its question is whether within-problem structural diversity
helps under the existing explicit absence-of-neutral-operation convention,
relative to globally matched single-path assignment. It is an allocation
contrast on selected legal trajectories. Absence still permits x/x cancellation,
computed ones and template shortcuts; it is not a semantic-strategy category.

Because both model conditions use only this family, there is no between-family
interaction to estimate here. Requiring a question also to admit four present
paths, eight classes across families, or equal length across families adds
selection with no role in this pair's control. Remove those requirements
explicitly. C011 remains a separate diagnosis of the abandoned double-family
restriction; C012 does not select the family with whichever new yield is largest.
A future between-family comparison would need its own common-population and
budget design, not an automatic comparison to old models or these results.

## Fixed inputs, finite search and matching

Use all 256 original training questions and targets and their full, previously
verified C009 tokenized archive. No new problem, target, tokenizer, rendering,
identity rule, K, solver grammar, adaptation recipe or evaluation outcome is
selected in this step. Numeric-leaf AC identity remains the fixed proxy.

For each question, find four identity-absent rows at a common EOS-inclusive
response length with four distinct structures and four different AC classes.
The other family imposes no eligibility constraint. Enumerate every four-
structure combination separately within each question/length; never stitch
structures across lengths. Shared keys require four distinct questions.
Each question uses its lowest feasible length for that key; sorted expressions
and augmenting paths determine representatives. Record every local feasible
question, all shared keys, witnesses, counters and a **120-second deadline**.
A deadline stop is incomplete, never absence. Original and new output bytes
are immutable; no higher-limit retry occurs inside this entry.

Compress keys with identical exact supporting question sets for packing, keeping
the lexicographically first structure key as representative. Solve maximum
number of disjoint four-question blocks with the common **60-second MILP**
formulation in C010: binary question/group assignment, integer block counts,
question assigned at most once, each group's assignments equal four times its
block count. Validate every returned integer assignment independently, retain
primal and dual bounds, and do not claim optimality after a solver limit.

## Training preparation gate, fixed before support outcomes

The family is identity_absent; paired assignment/order/training seed **31**,
evaluation seed17. Choose32 blocks/128 questions if a verified packing supplies
at least32; otherwise choose16 blocks/64 questions if available. Fewer than16
blocks means do not train this candidate. These are operational minimums, not
a power calculation or a claim that64 questions suffice for ICLR.

Sort the complete chosen packing by group/key/question identities, use the
fixed selection seed31 to shuffle, and take the largest permitted tier. Save
all selected and excluded IDs and compare the resulting population to the
original256 and each eligibility stage. This is random selection from that
specific feasible packing, not uniform sampling from all feasible packings.
The selected population can remain biased; do not claim the full256 distribution
is restored or reweight into strata with zero eligibility.

Keep1024 updates and effective batch4, microbatch2. With B selected blocks,
use C=256/B cycles: eight cycles for32 blocks or16 cycles for16 blocks. Every
block has four Latin rounds per cycle, so 4*B*C=1024. Thus each question has
equal exposures; no incomplete final cycle or uneven question reweighting is
used to hit the step count. Paths rotates all four paths; GCM repeats one seeded
assigned path per question. Every update must match within-pair structure
histograms, ordered question exposure, supervised tokens including EOS,
processed tokens and padding. Verify full canonical traces and labels.

If the gate passes, prepare immutable data, exact manifests, paired configs,
independent CPU verification and a bounded queue. Reuse pinned Qwen2.5-1.5B base,
full-parameter FP32 AdamW with BF16 autocast, LR5e-5, weight decay0.01, gradient
clip1, max sequence/generation384. Evaluate the unchanged two64-question
matched/broader development sets, greedy final correctness as primary and
complete trace/sample pass@k as separately labeled secondary endpoints. Do not
read their prior model predictions for selection; no holdout evaluation occurs.

Reserve **2*(1050+15)=2130 seconds** for the complete pair under the unchanged
7200-second allowance. There are2484 seconds remaining after4716 charged;
previous roughly500-second arm runtimes do not replace conservative bounds.
No four-model phase, extra budget or new rental is implied. Require the verified
ledger, current Git commit, original model bytes, idle GPU, at least18GiB free,
and the CPU data/queue checks before a later explicit launch. A new or cloned
server must not replay completed queues or reset the ledger.

## Scientific and launch decision

Record exact support, packing, representation of larger targets/input1/template
patterns and scheduled numerical residuals before declaring preparation ready.
A passing operational gate produces a limited boundary pilot, not an ICLR-ready
claim, causal identity removal, or independent validation of the earlier seeds.
If the gate fails, preserve the failure and provide a concrete revised design;
do not substitute a tiny training pool or another family after seeing yield.
The completed user-facing handoff must say clearly whether training artifacts
are ready, whether server startup is needed, and what limitations remain.

**Outcome at registration: pending.** No new support or model result has been
used to choose this candidate. Source publication and actual CPU receipts follow.
